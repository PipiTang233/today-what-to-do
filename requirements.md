# 需求文档：今天我想做什么

## 1. 项目概述
一款轻量、有趣、治愈的 Web 应用，帮助用户在两种常见的日常迷茫中找到灵感：
- 有事干但想在零碎时间放松
- 有时间但无聊，不知道干啥

通过 3-5 个简单问题引导，匹配活动池，随机推荐一件事去做。

## 2. 核心交互流程

```
首页 → 点击开始 → Q1: 你现在什么状态？
    ├─ 😮‍💨 有事干，想摸鱼放松
    │    ├─ Q2a: 你在哪？ → 工位 / 家里 / 外面
    │    │    └─ Q3a: 还能盯屏幕吗？ → 不想看了 / 可以看
    │    │         └─ Q4a: 现在什么感觉？ → 困 / 烦躁 / 平静
    │    │              └─ → 匹配活动池 → 随机抽一个 → 展示结果
    │
    └─ 🥱 有时间但无聊，不知道干啥
         ├─ Q2b: 想在家还是出门？ → 在家 / 出去走走
         │    └─ Q3b: 想动脑还是放空？ → 动脑 / 放空
         │         └─ Q4b: 一个人还是约人？ → 自己 / 找伴
         │              └─ Q5b: 有多少时间？ → 短时 / 一下午
         │                   └─ → 匹配活动池 → 随机抽一个 → 展示结果

结果页:
  - 显示活动名称 + emoji + 暖心描述
  - 按钮：「换一个」（同池子重抽）/「重新开始」
  - 按钮：「分享结果」（生成截图分享）
```

## 3. 功能需求

### 3.1 问答引导
- 每个问题 2-4 个选项，点击即进入下一题
- 页面有进度提示（当前第几题）
- 卡片切换有平滑动画

### 3.2 结果推荐
- 根据所有答案匹配对应的活动池（一个活动池包含多个活动）
- 从活动池中**随机抽取一个**作为结果
- 点击「换一个」从同池子重新随机抽取（可能抽到不同结果）
- 结果展示：**活动名称 + emoji + 一段暖心描述**
- 可点击「重新开始」回到首页

### 3.3 分享功能
- 将结果生成分享图片（截图模式）
- 图片包含：活动名称 + emoji + 描述 + 网站二维码
- 用户可保存图片或分享给朋友

### 3.4 界面要求
- UI 风格：糖果色鲜艳活泼，温柔可爱
- 移动端适配良好（手机和电脑都能用）
- 纯中文界面
- 使用通用 emoji，保证老手机兼容

## 4. 数据要求

### 4.1 活动数量
- 总活动数：45 个

### 4.2 活动数据结构
```
每个活动包含：
  - id: 唯一标识
  - name: 活动名称
  - emoji: 对应表情符号
  - description: 一段暖心描述（1-2句话）
  - pools: 所属活动池列表（匹配哪些路径）
```

### 4.3 活动池结构
每个叶子节点（问题路径终点）对应一个活动池（活动 ID 列表）。
B 路线按时间分为短时/一下午两个池，内容先相同，后续可独立调整。

## 5. 技术约束
- 纯前端实现，零后端、零数据库
- HTML + CSS + JavaScript（原生），无框架依赖
- 无 npm / Node.js 构建步骤
- 一个文件夹即完整项目，双击 index.html 可运行
- 可部署到 GitHub Pages 或任意静态服务器
- 二维码用 JS 本地生成，不依赖外部服务

## 6. 后续迭代（暂不实现）
- 历史记录 / 打卡统计
- 背景音乐 / 音效
- 自定义活动库
